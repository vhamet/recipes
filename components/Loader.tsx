import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faFireBurner } from "@fortawesome/free-solid-svg-icons";

const Loader = () => {
  return (
    <div>
      <FontAwesomeIcon icon={faFireBurner} bounce />
    </div>
  );
};

export default Loader;
